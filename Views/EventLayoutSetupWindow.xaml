<Window x:Class="BoothDesktop.Views.EventLayoutSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:BoothDesktop.Converters"
        Title="Layouts for this event"
        Width="640" Height="620"
        MinWidth="480" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.WindowBg}">
    <Window.Resources>
        <converters:FilePathToImageSourceConverter x:Key="FilePathToImage"/>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Visible layouts" FontSize="22" FontWeight="SemiBold"
                   Foreground="{StaticResource Brush.Text}"/>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,8,0,16"
                   Foreground="{StaticResource Brush.TextMuted}" FontSize="14"
                   Text="Checked layouts appear when guests open the booth. Use the preview and name to pick the right design."/>
        <Border Grid.Row="2" CornerRadius="12" Background="{StaticResource Brush.Card}"
                BorderBrush="{StaticResource Brush.Stroke}" BorderThickness="1" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="LayoutChecksList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4,6" Padding="10" CornerRadius="10"
                                    Background="#18FFFFFF" BorderBrush="{StaticResource Brush.Stroke}"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}"/>
                                    <Border Grid.Column="1" Width="88" Height="112" CornerRadius="8"
                                            ClipToBounds="True" Background="{StaticResource Brush.FakeLiveView}"
                                            BorderBrush="{StaticResource Brush.Stroke}" BorderThickness="1">
                                        <Grid>
                                            <Image Stretch="Uniform" Margin="4"
                                                   Source="{Binding PreviewPath, Converter={StaticResource FilePathToImage}}">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasPreview}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                            <TextBlock Text="No preview" FontSize="11" TextAlignment="Center"
                                                       TextWrapping="Wrap" MaxWidth="76"
                                                       Foreground="{StaticResource Brush.TextMuted}"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasPreview}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="14,0,0,0"
                                               Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold"
                                               Foreground="{StaticResource Brush.Text}" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Style="{StaticResource Style.GhostButton}" MinWidth="100" Margin="0,0,12,0" IsCancel="True" Click="OnCancelClick"/>
            <Button Content="Save" Style="{StaticResource Style.PrimaryButton}" MinWidth="100" IsDefault="True" Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>
